Put agent-specific Pi skills here.

Each skill should live in its own directory with a `SKILL.md` file.
Example:

```text
skills/
  gmail/
    SKILL.md
    scripts/
    references/
```

Pi will discover these skills through the agent-local `settings.json` file.
